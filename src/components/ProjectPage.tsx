import Link from "next/link";
import type { Project } from "@/types/project";
import { Artwork, Gallery } from "./Artwork";
import { ModelViewer } from "./ModelViewer";
import { VideoPlayer } from "./VideoPlayer";
export function ProjectPage({ project }: { project: Project }) { return <main className="project-page"><section className="project-hero"><div><p className="eyebrow">{project.category.join(" / ")} · {project.year}</p><h1>{project.title}</h1><p className="lede">{project.shortDescription}</p><dl><div><dt>Role</dt><dd>{project.role}</dd></div>{project.engine && <div><dt>Engine</dt><dd>{project.engine}</dd></div>}<div><dt>Software</dt><dd>{project.software.join(" · ")}</dd></div></dl></div><div className="thumb-frame"><Artwork src={project.heroImage} alt={`${project.title} hero placeholder`} />{project.logo && <img className="thumb-logo" src={project.logo} alt={`${project.title} logo`} />}</div></section><section className="split-section"><div><p className="eyebrow">Overview</p><h2>Built for the complete asset journey.</h2></div><p className="body-copy">{project.description}</p></section><section className="process"><p className="eyebrow">Process</p><div>{project.pipeline.map((step, i) => <article key={step.title}><span>{String(i + 1).padStart(2, "0")}</span><h3>{step.title}</h3><p>{step.note}</p></article>)}</div></section>{project.video && <VideoPlayer src={project.video} />}{project.model && <ModelViewer model={project.model} />}<section className="project-gallery"><p className="eyebrow">Gallery</p><Gallery items={project.gallery} /></section>{project.technical && <section className="technical"><p className="eyebrow">Technical breakdown</p><div>{Object.entries(project.technical).map(([key, value]) => <article key={key}><h3>{key}</h3><p>{Array.isArray(value) ? value.join(" · ") : value}</p></article>)}</div></section>}<Link className="text-link" href="/work">← All work</Link></main>; }
